export type PressedKeysObject = {
    left: boolean,
    right: boolean,
    counterClockwise: boolean,
    clockwise: boolean,
    oneEighty: boolean,
    hold: boolean,
    softDrop: boolean,
    hardDrop: boolean,
    reset: boolean,
}