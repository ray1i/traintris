export type SettingsObject = {
    das: number,
    arr: number,
    sd: number,
    left: string | null,
    right: string | null,
    counterClockwise: string | null,
    clockwise: string | null,
    oneEighty: string | null,
    hold: string | null,
    softDrop: string | null,
    hardDrop: string | null,
    reset: string | null,
}